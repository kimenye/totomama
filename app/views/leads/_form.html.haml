= form_for @lead do |f|
  - if @lead.errors.any?
    #error_explanation
      %h2= "#{pluralize(@lead.errors.count, "error")} prohibited this lead from being saved:"
      %ul
        - @lead.errors.full_messages.each do |msg|
          %li= msg
  .row
    .large-10.columns.large-centered.form-bg
      .large-6.columns.side-copy
        %img{:src => "/assets/mother-baby.png"}
          %h4 Welcome to Toto Mama
          %p Bla bla bla bla bla bla bla bla bla
      .large-6.columns.main_form
        %h4 We are launching soon, please fill in the form below and we will get back to you
        .field
          -#= f.label :name, :class => "dfd"
          = f.text_field :name, :placeholder => "Your Name"
        .field
          -#= f.label :email
          = f.text_field :email, :placeholder => "Your Email"
        .field
          -#= f.label :phone_number
          = f.text_field :phone_number, :placeholder => "Your Phone Number"
        .field
          -#= f.label :category
          = f.text_field :category, :placeholder => "Do you own a shop, sell baby stuff or just buying"
        .field
          -#= f.label :location
          = f.text_field :location, :placeholder => "Where are you located"
        .actions
          = f.submit 'Save'
